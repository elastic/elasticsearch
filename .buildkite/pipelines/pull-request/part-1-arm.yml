config:
  allow-labels: "test-arm"
steps:
  - label: part-1-arm
    command: .ci/scripts/run-gradle.sh -Dignore.tests.seed -Dtests.jvm.argline="-Des.entitlements.enabled=false" checkPart1
    timeout_in_minutes: 300
    agents:
      provider: aws
      imagePrefix: elasticsearch-ubuntu-2004-aarch64
      instanceType: m6g.8xlarge
      diskSizeGb: 350
      diskType: gp3
      diskName: /dev/sda1

---
setup:
  - skip:
      version: "all"
      reason:  "my reason"
---
"Test with setup and skip but no feature":
  - do:
      something:
        id: "something"
  - match: { acknowledged: true }



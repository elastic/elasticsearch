---
setup:
  - skip:
      features: warnings
---
"Test with existing single warnings":
  - do:
      warnings:
        - "a"
      something:
        id: "something"
  - match: { acknowledged: true }


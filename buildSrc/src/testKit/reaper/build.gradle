plugins {
  id 'elasticsearch.reaper'
}

tasks.register("launchReaper") {
  doLast {
    def reaper = project.extensions.getByName('reaper').reaperService.get()
    reaper.registerCommand('test', 'true')
    reaper.unregister('test')
  }
}

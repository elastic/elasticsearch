admin:
  cluster:
    - all
  indices:
    - names: '*'
      privileges:
        - all
      allow_restricted_indices: true
  run_as: [ '*' ]
  applications:
    - application: '*'
      privileges: [ '*' ]
      resources: [ '*' ]

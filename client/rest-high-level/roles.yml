admin:
  cluster:
    - all
  indices:
    - names: '*'
      allow_restricted_indices: true
      privileges:
        - all
  run_as: [ '*' ]
  applications:
    - application: '*'
      privileges: [ '*' ]
      resources: [ '*' ]

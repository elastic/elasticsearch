`*_node_http.json` contains files created by spining up toy clusters with a
few nodes in different configurations locally at various versions. They are
for testing `ElasticsearchHostsSniffer` against different versions of
Elasticsearch.

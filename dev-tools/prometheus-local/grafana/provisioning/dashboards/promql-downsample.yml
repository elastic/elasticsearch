apiVersion: 1

providers:
  - name: PromQL downsample
    orgId: 1
    folder: Elasticsearch
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards

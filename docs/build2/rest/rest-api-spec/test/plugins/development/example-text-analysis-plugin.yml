---
"line_139":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: would require this plugin to be installed
  - do:
      raw:
        method: GET
        path: "_analyze"
        body: |
          {
            "text": "hello to everyone except the world",
            "tokenizer": "standard",
            "filter":  ["hello_world"]
          }
  - is_false: _shards.failures

---
"line_28":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_autoscaling/policy/my_autoscaling_policy"
        body: |
          {
            "roles" : [ "data_hot" ],
            "deciders": {
              "proactive_storage": {
                "forecast_window": "10m"
              }
            }
          }
  - is_false: _shards.failures
  - match:
      $body:
        {
          "acknowledged": true
        }
  - do:
      raw:
        method: DELETE
        path: "_autoscaling/policy/my_autoscaling_policy"
  - is_false: _shards.failures

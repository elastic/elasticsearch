---
"line_82":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_cat/nodeattrs"
        v: "true"
        s: "node,attr"
  - match:
      $body:
        /node    \s+host      \s+ip        \s+attr     \s+value\s*
         (.+ \s+ml\..+\s*
         )*node-0 \s+127.0.0.1 \s+127.0.0.1 \s+testattr \s+test\s*
         \s*
         (.+ \s+xpack\.installed \s+true\s*
         )?\s*/
---
"line_113":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_cat/nodeattrs"
        v: "true"
        h: "name,pid,attr,value"
        s: "node,attr"
  - match:
      $body:
        /name    \s+pid   \s+attr     \s+value\s*
         (.+ \s+ml\..+\s*
         )*node-0 \s+\d* \s+testattr \s+test\s*
         \s*
         (.+ \s+xpack\.installed \s+true\s*
         )?\s*/

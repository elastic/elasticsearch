---
"line_52":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_cat/pending_tasks"
        v: "true"
  - match:
      $body:
        /insertOrder \s+timeInQueue \s+priority \s+source(\s*
         .+)*\s*/

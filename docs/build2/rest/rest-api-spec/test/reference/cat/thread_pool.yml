---
"line_136":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_cat/thread_pool"
  - match:
      $body:
        /node-\d+ \s+analyze             \s+\d+ \s+\d+ \s+\d+\s*
         (node-\d+ \s+\S+ \s+\d+ \s+\d+ \s+\d+\s*
         )*\s*
         node-\d+ \s+fetch_shard_started \s+\d+ \s+\d+ \s+\d+\s*
         node-\d+ \s+fetch_shard_store   \s+\d+ \s+\d+ \s+\d+\s*
         node-\d+ \s+flush               \s+\d+ \s+\d+ \s+\d+\s*
         (node-\d+ \s+\S+ \s+\d+ \s+\d+ \s+\d+\s*
         )*\s*
         node-\d+ \s+write               \s+\d+ \s+\d+ \s+\d+\s*/
---
"line_172":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_cat/thread_pool/generic"
        v: "true"
        h: "id,name,active,rejected,completed"
  - match:
      $body:
        /id                     \s+name    \s+active \s+rejected \s+completed\s*
         [\w-]+ \s+generic      \s+\d+        \s+\d+        \s+\d+\s*/

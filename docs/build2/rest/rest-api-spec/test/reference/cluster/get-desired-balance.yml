---
"line_21":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: Can't reliably test desired balance
  - do:
      raw:
        method: GET
        path: "_internal/desired_balance"
  - is_false: _shards.failures

---
setup:
  - do:
      raw:
        method: PUT
        path: "_internal/desired_nodes/my_history/1"
        body: |
          {
              "nodes" : [
                  {
                      "settings" : {
                           "node.name" : "instance-000187",
                           "node.external_id": "instance-000187",
                           "node.roles" : ["data_hot", "master"],
                           "node.attr.data" : "hot",
                           "node.attr.logical_availability_zone" : "zone-0"
                      },
                      "processors" : 8.0,
                      "memory" : "59gb",
                      "storage" : "2tb"
                  }
              ]
          }
---
teardown:
  - do:
      raw:
        method: DELETE
        path: "_internal/desired_nodes"
---
"line_48":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_internal/desired_nodes/_latest"
  - is_false: _shards.failures
---
"line_64":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_internal/desired_nodes/_latest"
  - is_false: _shards.failures
  - match:
      $body:
        {
            "history_id": $body.history_id,
            "version": $body.version,
            "nodes": [
                {
                    "settings": $body.nodes.0.settings,
                    "processors": $body.nodes.0.processors,
                    "memory": "$body.nodes.0.memory",
                    "storage": "$body.nodes.0.storage"
                }
            ]
        }

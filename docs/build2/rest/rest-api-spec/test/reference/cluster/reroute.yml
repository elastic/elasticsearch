---
"line_195":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: doc tests run with only a single node
  - do:
      raw:
        method: POST
        path: "_cluster/reroute"
        metric: "none"
        body: |
          {
            "commands": [
              {
                "move": {
                  "index": "test", "shard": 0,
                  "from_node": "node1", "to_node": "node2"
                }
              },
              {
                "allocate_replica": {
                  "index": "test", "shard": 1,
                  "node": "node3"
                }
              }
            ]
          }
  - is_false: _shards.failures

---
"line_42":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: there's no way to clean up after creating a connector sync job, as we don't know the id ahead of time. Therefore, skip this test.
  - do:
      raw:
        method: GET
        path: "_connector/_sync_job/my-connector-sync-job"
  - is_false: _shards.failures

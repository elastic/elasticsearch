---
"line_132":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: no cloud template
  - do:
      raw:
        method: DELETE
        path: "_template/.cloud-hot-warm-allocation-0"
  - is_false: _shards.failures
---
"line_164":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my-index"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my-index/_settings"
        body: |
          {
            "index.routing.allocation.require.data": "hot"
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my-index/_settings"
        body: |
          {
            "index.routing.allocation.require.data": null,
            "index.routing.allocation.include._tier_preference": "data_hot"
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my-index/_settings"
        body: |
          {
            "index.routing.allocation.require.data": null,
            "index.routing.allocation.include._tier_preference": "data_warm,data_hot"
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my-index/_settings"
        body: |
          {
            "index.routing.allocation.require.data": null,
            "index.routing.allocation.include._tier_preference": "data_warm,data_hot"
          }
  - is_false: _shards.failures

---
"line_87":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "test/_doc/1"
        refresh: ""
        body: |
          {"test": "test"}
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "test/_doc/2"
        refresh: "true"
        body: |
          {"test": "test"}
  - is_false: _shards.failures
---
"line_98":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "test/_doc/3"
        body: |
          {"test": "test"}
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "test/_doc/4"
        refresh: "false"
        body: |
          {"test": "test"}
  - is_false: _shards.failures
---
"line_108":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "test/_doc/4"
        refresh: "wait_for"
        body: |
          {"test": "test"}
  - is_false: _shards.failures

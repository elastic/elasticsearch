---
"line_60":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_ilm/status"
  - is_false: _shards.failures
  - match:
      $body:
        {
          "operation_mode": "RUNNING"
        }

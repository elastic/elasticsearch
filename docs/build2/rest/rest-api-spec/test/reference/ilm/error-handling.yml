---
"line_15":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_ilm/policy/shrink-index"
        body: |
          {
            "policy": {
              "phases": {
                "warm": {
                  "min_age": "5d",
                  "actions": {
                    "shrink": {
                      "number_of_shards": 4
                    }
                  }
                }
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my-index-000001"
        body: |
          {
            "settings": {
              "index.number_of_shards": 2,
              "index.lifecycle.name": "shrink-index"
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: "my-index-000001/_ilm/explain"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "_ilm/policy/shrink-index"
        body: |
          {
            "policy": {
              "phases": {
                "warm": {
                  "min_age": "5d",
                  "actions": {
                    "shrink": {
                      "number_of_shards": 1
                    }
                  }
                }
              }
            }
          }
  - is_false: _shards.failures
---
"line_144":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: we can't be sure the index is ready to be retried at this point
  - do:
      raw:
        method: POST
        path: "my-index-000001/_ilm/retry"
  - is_false: _shards.failures

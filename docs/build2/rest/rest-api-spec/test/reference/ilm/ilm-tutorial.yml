---
"line_73":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_ilm/policy/timeseries_policy"
        body: |
          {
            "policy": {
              "phases": {
                "hot": {
                  "actions": {
                    "rollover": {
                      "max_primary_shard_size": "50GB",
                      "max_age": "30d"
                    }
                  }
                },
                "delete": {
                  "min_age": "90d",
                  "actions": {
                    "delete": {}
                  }
                }
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "_index_template/timeseries_template"
        body: |
          {
            "index_patterns": ["timeseries"],
            "data_stream": { },
            "template": {
              "settings": {
                "number_of_shards": 1,
                "number_of_replicas": 1,
                "index.lifecycle.name": "timeseries_policy"
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "timeseries/_doc"
        body: |
          {
            "message": "logged the request",
            "@timestamp": "1591890611"
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: ".ds-timeseries-*/_ilm/explain"
  - is_false: _shards.failures
  - do:
      raw:
        method: DELETE
        path: "_data_stream/timeseries"
  - is_false: _shards.failures
  - do:
      raw:
        method: DELETE
        path: "_index_template/timeseries_template"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "_index_template/timeseries_template"
        body: |
          {
            "index_patterns": ["timeseries-*"],
            "template": {
              "settings": {
                "number_of_shards": 1,
                "number_of_replicas": 1,
                "index.lifecycle.name": "timeseries_policy",
                "index.lifecycle.rollover_alias": "timeseries"
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: DELETE
        path: "_index_template/timeseries_template"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "timeseries-000001"
        body: |
          {
            "aliases": {
              "timeseries": {
                "is_write_index": true
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: "timeseries-*/_ilm/explain"
  - is_false: _shards.failures
  - do:
      raw:
        method: DELETE
        path: "timeseries-000001"
  - is_false: _shards.failures

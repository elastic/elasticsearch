---
"line_54":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "test"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "test/_settings"
        body: |
          {
            "index.routing.allocation.include.size": "big,medium"
          }
  - is_false: _shards.failures
---
"line_74":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "test"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "test/_settings"
        body: |
          {
            "index.routing.allocation.require.size": "big",
            "index.routing.allocation.require.rack": "rack1"
          }
  - is_false: _shards.failures
---
"line_122":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: indexes don't assign
  - do:
      raw:
        method: PUT
        path: "test/_settings"
        body: |
          {
            "index.routing.allocation.include._ip": "192.168.2.*"
          }
  - is_false: _shards.failures

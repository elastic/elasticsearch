---
"line_10":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my-index-000001"
        body: |
          {"settings":{"index.number_of_shards" : 5,"blocks.write":true}}
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my-index-000001/_clone/cloned-my-index-000001"
  - is_false: _shards.failures
---
"line_39":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my_source_index"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my_source_index/_block/write"
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my_source_index/_clone/my_target_index"
  - is_false: _shards.failures
---
"line_117":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my_source_index"
        body: |
          {"settings": {"index.blocks.write": true, "index.number_of_shards": "5"}}
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my_source_index/_clone/my_target_index"
        body: |
          {
            "settings": {
              "index.number_of_shards": 5
            },
            "aliases": {
              "my_search_indices": {}
            }
          }
  - is_false: _shards.failures

---
"line_13":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: Difficult to set up
  - do:
      raw:
        method: POST
        path: "_dangling/%3Cindex-uuid%3E"
        accept_data_loss: "true"
  - is_false: _shards.failures
---
"line_59":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: Difficult to set up
  - do:
      raw:
        method: POST
        path: "_dangling/zmM4e0JtBkeUjiHD-MihPQ"
        accept_data_loss: "true"
  - is_false: _shards.failures
  - match:
      $body:
        {
          "acknowledged" : true
        }

---
setup:
  - do:
      raw:
        method: PUT
        path: "_index_template/my-index-template"
        body: |
          {
           "index_patterns" : ["te*"],
           "template": {
               "settings": {
              "number_of_shards": 1
            }
           }
          }
---
"line_26":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: DELETE
        path: "_index_template/my-index-template"
  - is_false: _shards.failures

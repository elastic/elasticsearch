---
"line_11":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my-index-000001"
        body: |
          {"settings":{"index.number_of_shards":2,"blocks.write":true}}
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my-index-000001/_shrink/shrunk-my-index-000001"
  - is_false: _shards.failures
---
"line_46":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my_source_index"
        body: |
          {"settings":{"index.number_of_shards":2}}
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my_source_index/_settings"
        body: |
          {
            "settings": {
              "index.number_of_replicas": 0,
              "index.routing.allocation.require._name": "shrink_node_name"
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my_source_index/_block/write"
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my_source_index/_shrink/my_target_index"
        body: |
          {
            "settings": {
              "index.routing.allocation.require._name": null,
              "index.blocks.write": null
            }
          }
  - is_false: _shards.failures
---
"line_162":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my_source_index"
        body: |
          {"settings": {"index.number_of_shards":5,"index.blocks.write": true}}
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my_source_index/_shrink/my_target_index"
        body: |
          {
            "settings": {
              "index.number_of_replicas": 1,
              "index.number_of_shards": 1,
              "index.codec": "best_compression"
            },
            "aliases": {
              "my_search_indices": {}
            }
          }
  - is_false: _shards.failures

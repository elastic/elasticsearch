---
"line_10":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my-index-000001"
        body: |
          {"settings":{"blocks.write":true}}
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my-index-000001/_split/split-my-index-000001"
        body: |
          {
            "settings": {
              "index.number_of_shards": 2
            }
          }
  - is_false: _shards.failures
---
"line_44":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my_source_index"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my_source_index/_block/write"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my-index-000001"
        body: |
          {
            "settings": {
              "index": {
                "number_of_routing_shards": 30
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my_source_index/_split/my_target_index"
        body: |
          {
            "settings": {
              "index.number_of_shards": 2
            }
          }
  - is_false: _shards.failures
---
"line_203":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my_source_index"
        body: |
          {"settings": {"index.blocks.write": true, "index.number_of_shards": "1"}}
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "my_source_index/_split/my_target_index"
        body: |
          {
            "settings": {
              "index.number_of_shards": 5
            },
            "aliases": {
              "my_search_indices": {}
            }
          }
  - is_false: _shards.failures

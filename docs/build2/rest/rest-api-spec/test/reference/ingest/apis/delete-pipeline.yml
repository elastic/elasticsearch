---
setup:
  - do:
      raw:
        method: PUT
        path: "_ingest/pipeline/my-pipeline-id"
        body: |
          {
            "description" : "example pipeline to delete",
            "processors" : [ ]
          }
  - do:
      raw:
        method: PUT
        path: "_ingest/pipeline/pipeline-one"
        body: |
          {
            "description" : "another example pipeline to delete",
            "processors" : [ ]
          }
---
"line_28":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: DELETE
        path: "_ingest/pipeline/my-pipeline-id"
  - is_false: _shards.failures
---
"line_73":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: DELETE
        path: "_ingest/pipeline/pipeline-one"
  - is_false: _shards.failures
---
"line_82":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: DELETE
        path: "_ingest/pipeline/pipeline-*"
  - is_false: _shards.failures
---
"line_91":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: DELETE
        path: "_ingest/pipeline/*"
  - is_false: _shards.failures
  - match:
      $body:
        {
        "acknowledged": true
        }

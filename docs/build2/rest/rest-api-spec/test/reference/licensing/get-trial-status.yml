---
"line_40":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_license/trial_status"
  - is_false: _shards.failures
  - match:
      $body:
        {
          "eligible_to_start_trial": $body.eligible_to_start_trial
        }

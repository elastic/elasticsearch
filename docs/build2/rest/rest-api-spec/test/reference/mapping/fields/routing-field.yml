---
setup:
  - do:
      raw:
        method: PUT
        path: "my-index-000001/_doc/1"
        routing: "user1"
        refresh: "true"
        body: |
          {
            "title": "This is a document"
          }
  - do:
      raw:
        method: GET
        path: "my-index-000001/_doc/1"
        routing: "user1"
---
"line_38":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "my-index-000001/_search"
        body: |
          {
            "query": {
              "terms": {
                "_routing": [ "user1" ]
              }
            }
          }
  - is_false: _shards.failures
---
"line_62":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "my-index-000001/_search"
        routing: "user1,user2"
        body: |
          {
            "query": {
              "match": {
                "title": "document"
              }
            }
          }
  - is_false: _shards.failures
---
"line_87":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my-index-000002"
        body: |
          {
            "mappings": {
              "_routing": {
                "required": true
              }
            }
          }
  - is_false: _shards.failures
  - do:
      catch: bad_request
      raw:
        method: PUT
        path: "my-index-000002/_doc/1"
        body: |
          {
            "text": "No routing value provided"
          }
  - is_false: _shards.failures

---
"line_24":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my-index-000001"
        body: |
          {
            "mappings": {
              "_source": {
                "enabled": false
              }
            }
          }
  - is_false: _shards.failures
---
"line_80":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "logs"
        body: |
          {
            "mappings": {
              "_source": {
                "includes": [
                  "*.count",
                  "meta.*"
                ],
                "excludes": [
                  "meta.description",
                  "meta.other.*"
                ]
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "logs/_doc/1"
        body: |
          {
            "requests": {
              "count": 10,
              "foo": "bar"
            },
            "meta": {
              "name": "Some metric",
              "description": "Some metric description",
              "other": {
                "foo": "one",
                "baz": "two"
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: "logs/_search"
        body: |
          {
            "query": {
              "match": {
                "meta.other.foo": "one"
              }
            }
          }
  - is_false: _shards.failures

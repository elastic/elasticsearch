---
"line_12":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "trips"
        body: |
          {
            "mappings": {
              "properties": {
                "distance": {
                  "type": "long"
                },
                "route_length_miles": {
                  "type": "alias",
                  "path": "distance"
                },
                "transit_mode": {
                  "type": "keyword"
                }
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: "_search"
        body: |
          {
            "query": {
              "range" : {
                "route_length_miles" : {
                  "gte" : 39
                }
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: "trips/_field_caps"
        fields: "route_*,transit_mode"
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: "_search"
        body: |
          {
            "query" : {
              "match_all": {}
            },
            "_source": "route_length_miles"
          }
  - is_false: _shards.failures

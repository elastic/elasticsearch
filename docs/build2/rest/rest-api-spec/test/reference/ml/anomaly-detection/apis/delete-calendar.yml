---
"line_38":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: setup:calendar_outages
  - do:
      raw:
        method: DELETE
        path: "_ml/calendars/planned-outages"
  - is_false: _shards.failures
  - match:
      $body:
        {
          "acknowledged": true
        }

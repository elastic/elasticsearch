---
"line_74":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: Kibana sample data
  - do:
      raw:
        method: POST
        path: "_ml/datafeeds/datafeed-low_request_rate/_stop"
        body: |
          {
            "timeout": "30s"
          }
  - match:
      $body:
        {
          "stopped": true
        }

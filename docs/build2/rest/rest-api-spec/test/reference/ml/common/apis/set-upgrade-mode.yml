---
teardown:
  - do:
      raw:
        method: POST
        path: "_ml/set_upgrade_mode"
        enabled: "false"
        timeout: "10m"
---
"line_74":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: POST
        path: "_ml/set_upgrade_mode"
        enabled: "true"
        timeout: "10m"
  - is_false: _shards.failures
  - match:
      $body:
        {
          "acknowledged": true
        }
---
"line_96":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: POST
        path: "_ml/set_upgrade_mode"
        enabled: "false"
        timeout: "10m"
  - is_false: _shards.failures

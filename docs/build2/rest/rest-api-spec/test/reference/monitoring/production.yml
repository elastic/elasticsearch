---
"line_52":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: security errs
  - do:
      raw:
        method: GET
        path: "_cluster/settings"
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "_cluster/settings"
        body: |
          {
            "persistent": {
              "xpack.monitoring.collection.enabled": false
            }
          }
  - is_false: _shards.failures
---
"line_94":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: needs-gold+-license
  - do:
      raw:
        method: POST
        path: "_security/user/remote_monitor"
        body: |
          {
            "password" : "changeme",
            "roles" : [ "remote_monitoring_agent"],
            "full_name" : "Internal Agent For Remote Monitoring"
          }
  - is_false: _shards.failures

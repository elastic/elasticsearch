---
"line_63":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: POST
        path: "_security/service/elastic/fleet-server/credential/token/token1"
  - is_false: _shards.failures
  - match:
      $body:
        {
          "created": true,
          "token": {
            "name": "token1",
            "value": "$body.token.value"
          }
        }
---
"line_97":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: POST
        path: "_security/service/elastic/fleet-server/credential/token"
  - is_false: _shards.failures
  - match:
      $body:
        {
          "created": true,
          "token": {
            "name": "$body.token.name",
            "value": "$body.token.value"
          }
        }

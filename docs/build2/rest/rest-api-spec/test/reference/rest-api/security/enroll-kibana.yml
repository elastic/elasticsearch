---
"line_28":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: we need to enable HTTP TLS for the docs cluster
  - do:
      raw:
        method: GET
        path: "_security/enroll/kibana"
  - is_false: _shards.failures

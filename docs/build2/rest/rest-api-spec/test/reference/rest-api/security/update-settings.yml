---
"line_49":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: making sure all the indices have been created reliably is difficult
  - do:
      raw:
        method: PUT
        path: "_security/settings"
        body: |
          {
              "security": {
                  "index.auto_expand_replicas": "0-all"
              },
              "security-tokens": {
                  "index.auto_expand_replicas": "0-all"
              },
              "security-profile": {
                  "index.auto_expand_replicas": "0-all"
              }
          }
  - is_false: _shards.failures

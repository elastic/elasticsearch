---
"line_12":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_scripts/my-stored-script"
        body: |
          {
            "script": {
              "lang": "painless",
              "source": "Math.log(_score * 2) + params['my_modifier']"
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: GET
        path: "_scripts/my-stored-script"
  - is_false: _shards.failures
  - do:
      raw:
        method: DELETE
        path: "_scripts/my-stored-script"
  - is_false: _shards.failures

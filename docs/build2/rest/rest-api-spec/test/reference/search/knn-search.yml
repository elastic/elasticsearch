---
"line_13":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "my-index"
        body: |
          {
            "mappings": {
              "properties": {
                "image_vector": {
                  "type": "dense_vector",
                  "dims": 3,
                  "index": true,
                  "similarity": "l2_norm"
                }
              }
            }
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: PUT
        path: "my-index/_doc/1"
        refresh: ""
        body: |
          {
            "image_vector" : [0.5, 10, 6]
          }
  - is_false: _shards.failures
  - do:
      warnings:
         - "The kNN search API has been replaced by the `knn` option in the search API."
      raw:
        method: GET
        path: "my-index/_knn_search"
        body: |
          {
            "knn": {
              "field": "image_vector",
              "query_vector": [0.3, 0.1, 1.2],
              "k": 10,
              "num_candidates": 100
            },
            "_source": ["name", "file_type"]
          }
  - is_false: _shards.failures

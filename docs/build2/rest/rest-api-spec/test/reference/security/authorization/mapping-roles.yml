---
"line_138":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_security/role_mapping/admins"
        body: |
          {
            "roles" : [ "monitoring", "user" ],
            "rules" : { "field" : { "groups" : "cn=admins,dc=example,dc=com" } },
            "enabled": true
          }
  - is_false: _shards.failures
---
"line_148":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_security/role_mapping/basic_users"
        body: |
          {
            "roles" : [ "user" ],
            "rules" : { "any" : [
                { "field" : { "dn" : "cn=John Doe,cn=contractors,dc=example,dc=com" } },
                { "field" : { "groups" : "cn=users,dc=example,dc=com" } }
            ] },
            "enabled": true
          }
  - is_false: _shards.failures
---
"line_180":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_security/role_mapping/admin_user"
        body: |
          {
            "roles" : [ "monitoring" ],
            "rules" : { "field" : { "dn" : "cn=Admin,ou=example,o=com" } },
            "enabled": true
          }
  - is_false: _shards.failures
---
"line_190":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: PUT
        path: "_security/role_mapping/basic_user"
        body: |
          {
            "roles" : [ "user" ],
            "rules" : { "field" : { "dn" : "cn=John Doe,ou=example,o=com" } },
            "enabled": true
          }
  - is_false: _shards.failures

---
"line_32":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: POST
        path: "_security/user/user1"
        refresh: "true"
        body: |
          {
            "password" : "l0ng-r4nd0m-p@ssw0rd",
            "roles": ["admin"]
          }
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "_security/user/user1/_password"
        body: |
          {
            "password" : "new-test-password"
          }
  - is_false: _shards.failures

---
"line_109":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
        - always_skip
      reason: this would break the test cluster if executed
  - do:
      raw:
        method: POST
        path: "_cluster/voting_config_exclusions"
        node_names: "node_name"
  - is_false: _shards.failures
  - do:
      raw:
        method: POST
        path: "_cluster/voting_config_exclusions"
        node_names: "node_name"
        timeout: "1m"
  - is_false: _shards.failures
---
"line_152":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: GET
        path: "_cluster/state"
        filter_path: "metadata.cluster_coordination.voting_config_exclusions"
  - is_false: _shards.failures
---
"line_170":
  - skip:
      features:
        - default_shards
        - stash_in_key
        - stash_in_path
        - stash_path_replace
        - warnings
  - do:
      raw:
        method: DELETE
        path: "_cluster/voting_config_exclusions"
  - is_false: _shards.failures
  - do:
      raw:
        method: DELETE
        path: "_cluster/voting_config_exclusions"
        wait_for_removal: "false"
  - is_false: _shards.failures

pr: 113825
summary: Implements cross-cluster search telemetry, which is returned as "ccs" field in `_cluster/stats` output. Adds new parameter `include_remotes=true` to the `_cluster/stats` API which will produce data from connected remote clusters.
area: Search
type: feature
issues: []

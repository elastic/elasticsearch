pr: 158988
summary: "On a dataset the standard metadata columns answer what they name: `_index`,\
  \ `_id`, `_version` and `_source` bind and return null and `_score` is zero, while\
  \ `mappings._id` is refused on PUT; `_class` and `_name` are added and answer\
  \ uniformly on indices and datasets"
area: ES|QL
type: enhancement
issues: []

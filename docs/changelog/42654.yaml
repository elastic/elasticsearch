pr: 42654
issues:
  - 37096
area: Search
type: breaking
summary: Remove `CommonTermsQuery` and `cutoff_frequency` param
versions:
  - v8.0.0
breaking:
  notable: false
  title: Remove `CommonTermsQuery` and `cutoff_frequency` param
  anchor: remove_commontermsquery_and_cutoff_frequency_param
  body: >-
    Remove `common` query and `cutoff_frequency` parameter of`match` and
    `multi_match` queries. Both have already beendeprecated in 7.x.

    Closes: #37096

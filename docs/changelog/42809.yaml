pr: 42809
issues:
  - 27098
area: Search
type: breaking
summary: "Remove deprecated sort options: `nested_path` and `nested_filter`"
versions:
  - v8.0.0
breaking:
  notable: false
  title: "Remove deprecated sort options: `nested_path` and `nested_filter`"
  anchor: remove_deprecated_sort_options_nested_path_and_nested_filter_
  body: >-
    This commit removes the nested_path and nested_filter options deprecated in
    6x.

    This change also checks that the sort field has a [nested] option if it is under a nested

    object and throws an exception if it's not the case.

    Closes #27098

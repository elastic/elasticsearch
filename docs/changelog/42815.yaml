pr: 42815
issues: []
area: Features/Watcher
type: breaking-java
summary: Remove `WatcherClient` from x-pack
versions:
  - v8.0.0
breaking:
  notable: false
  title: Remove `WatcherClient` from x-pack
  anchor: remove_watcherclient_from_x_pack
  body: This commit removes the WatcherClient and WatcherRestHandler from
    thecodebase. The WatcherClient was a convenience wrapper around thetransport
    client, which is being removed so the client no longer servesa purpose. The
    WatcherRestHandler is no longer needed as its primarypurpose was to provide
    a WatcherClient to the implementing handlers.

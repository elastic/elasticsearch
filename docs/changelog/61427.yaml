pr: 61427
issues: []
area: Infra/REST API, SQL
type: breaking
summary: Allow parsing Content-Type and Accept headers with version
versions:
  - v8.0.0
breaking:
  notable: false
  title: Allow parsing Content-Type and Accept headers with version
  anchor: allow_parsing_content_type_and_accept_headers_with_version
  body: >-
    Content-Type and Accept headers expect a versioned form of media typeslike
    application/vnd.elasticsearch+json;compatible-with=7when previously it was
    simple application/json or (cbor, yaml..) - it is stillsupported

    next step after https://github.com/elastic/elasticsearch/pull/61987relates https://github.com/elastic/elasticsearch/pull/60516

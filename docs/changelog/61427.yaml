pr: 61427
issues:
  - 61987
  - 60516
area: Infra/REST API, SQL
type: breaking
summary: Allow parsing Content-Type and Accept headers with version
versions:
  - v8.0.0
breaking:
  notable: false
  title: Allow parsing Content-Type and Accept headers with version
  anchor: allow_parsing_content_type_and_accept_headers_with_version
  body: >-
    Content-Type and Accept headers expect a versioned form of media types

    like application/vnd.elasticsearch+json;compatible-with=7

    when previously it was simple application/json or (cbor, yaml..) - it is still

    supported

    next step after #61987

    relates #60516

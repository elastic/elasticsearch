pr: 65753
issues:
  - 65601
  - 65249
area: Network
type: breaking
summary: Remove escape hatch permitting incompatible builds
versions:
  - v8.0.0
breaking:
  notable: false
  title: Remove escape hatch permitting incompatible builds
  anchor: remove_escape_hatch_permitting_incompatible_builds
  body: >-
    Today in `7.x` there is a deprecated system property that bypasses thecheck
    that prevents nodes of incompatible builds from communicating.This commit
    removes the system property in `master` so that the check isalways enforced.

    Relates #65601, #65249

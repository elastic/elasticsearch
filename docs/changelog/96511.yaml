pr: 96511
summary: Do not return Cluster State in /_cluster/reroute API response
area: Allocation
type: "breaking, tech debt"
issues:
 - 88978
breaking:
  title: Do not return Cluster State in /_cluster/reroute API response
  area: REST API
  details: |-
    `state` field is not present in /_cluster/reroute API response. It was deprecated in previous release and now
    is removed.
  impact: Users of /_cluster/reroute API won't be able to read `state` field in /_cluster/reroute
    API response.
  notable: false

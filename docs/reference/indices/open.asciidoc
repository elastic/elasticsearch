[[indices-open]]
== Open index API
++++
<titleabbrev>Open index</titleabbrev>
++++

Opens a closed index.

[float]
[[indices-open-request]]
=== {api-request-title}

`POST /<index>/_open`

[float]
[[indices-open-desc]]
=== {api-description-title}

You can use the open index API to open a <<indices-close,closed index>>.

You can close an index using the <<indices-close,close index API>>. {es} blocks
read and write operations for closed indices. You cannot index or search for
documents in closed indices.

When opening an index, the <<master-node,master node>> restarts the index shards
to reflect the new state of the index. The index shards then go through the
<<indices-recovery,index recovery process>>. The cluster automatically copies
the data of opening indices to ensure a safe number of shard copies at all
times.

[float]
[[indices-open-path-params]]
=== {api-path-parms-title}

`<index>` (Required)::
+
--
(string) Comma-separated list or wildcard expression of index names to retrieve.

You can use a value of `_all` or `*` to open all indices.
--

[float]
[[indices-open-query-params]]
=== {api-query-parms-title}

`allow_no_indices` (Optional)::
(boolean) Indicates whether the operation is skipped if a wildcard expression
matches no indices. Defaults to `false`.

`expand_wildcards` (Optional)::
(string) Indicates whether wildcard expressions should expand to
open or closed indices. Possible values are:

* `closed` (Default)
* `open` 
* `none`
* `all`

`ignore_unavailable` (Optional)::
(boolean) Indicates whether unavailable indices are skipped. Defaults to
`false`.

`wait_for_active_shards` (Optional)::
(integer) Number of active shard copies required to start before returning a
response. See <<index-wait-for-active-shards>>.

include::{docdir}/rest-api/timeoutparms.asciidoc[]

[float]
[[indices-open-example]]
=== {api-example-title}

[source,js]
----
POST /my_index/_open
----
// CONSOLE
// TEST[s/^/PUT my_index\nPOST my_index\/_close\n/]

The API returns the following response:

[source,js]
----
{
    "acknowledged" : true,
    "shards_acknowledged" : true
}
----
// TESTRESPONSE
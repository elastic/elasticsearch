## Logical operators [esql-logical-operators]

The following logical operators are supported:

* `AND`
* `OR`
* `NOT`

To learn how `NULL` affects boolean logic and filtering, refer to [Working with NULL values in {{esql}}](/reference/query-languages/esql/esql-null-values.md).

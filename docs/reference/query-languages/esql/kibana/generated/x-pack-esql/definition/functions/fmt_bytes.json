{
  "comment" : "This is generated by ESQL's FmtBytesTests. Do not edit it. See docs/reference/query-languages/esql/README.md for how to regenerate it.",
  "type" : "scalar",
  "name" : "fmt_bytes",
  "description" : "Returns a human-readable representation of a byte count using binary (base-1024) units.\nFor example, `1536` becomes `\"1.5kb\"`.\nSupported units: `b`, `kb`, `mb`, `gb`, `tb`, `pb`. If a `unit` is provided, the output\nis pinned to that unit instead of auto-scaling to the largest unit that fits.",
  "signatures" : [
    {
      "params" : [
        {
          "name" : "bytes",
          "type" : "integer",
          "optional" : false,
          "description" : "The number of bytes to format. If `null`, the function returns `null`."
        }
      ],
      "variadic" : false,
      "returnType" : "keyword"
    },
    {
      "params" : [
        {
          "name" : "bytes",
          "type" : "integer",
          "optional" : false,
          "description" : "The number of bytes to format. If `null`, the function returns `null`."
        },
        {
          "name" : "unit",
          "type" : "keyword",
          "optional" : true,
          "description" : "The unit to pin the output to: one of `b`, `kb`, `mb`, `gb`, `tb`, `pb`. If omitted, the largest unit that keeps the value at least 1 is used.",
          "hint" : {
            "kind" : "constant",
            "allowedValues" : [
              "b",
              "kb",
              "mb",
              "gb",
              "tb",
              "pb"
            ]
          }
        }
      ],
      "variadic" : false,
      "returnType" : "keyword"
    },
    {
      "params" : [
        {
          "name" : "bytes",
          "type" : "long",
          "optional" : false,
          "description" : "The number of bytes to format. If `null`, the function returns `null`."
        }
      ],
      "variadic" : false,
      "returnType" : "keyword"
    },
    {
      "params" : [
        {
          "name" : "bytes",
          "type" : "long",
          "optional" : false,
          "description" : "The number of bytes to format. If `null`, the function returns `null`."
        },
        {
          "name" : "unit",
          "type" : "keyword",
          "optional" : true,
          "description" : "The unit to pin the output to: one of `b`, `kb`, `mb`, `gb`, `tb`, `pb`. If omitted, the largest unit that keeps the value at least 1 is used.",
          "hint" : {
            "kind" : "constant",
            "allowedValues" : [
              "b",
              "kb",
              "mb",
              "gb",
              "tb",
              "pb"
            ]
          }
        }
      ],
      "variadic" : false,
      "returnType" : "keyword"
    }
  ],
  "examples" : [
    "ROW bytes = 1536\n| EVAL formatted = FMT_BYTES(bytes)"
  ],
  "preview" : true,
  "snapshot_only" : false
}

{
  "comment" : "This is generated by ESQL's ToRangeTests. Do not edit it. See docs/reference/query-languages/esql/README.md for how to regenerate it.",
  "type" : "scalar",
  "name" : "to_range",
  "description" : "Constructs a range from two boundary values. The first argument is the inclusive lower bound; the second is the exclusive upper bound, following the half-open `[from, to)` convention used for all range types in ES|QL. Currently accepts `datetime` arguments and returns a `date_range`.",
  "signatures" : [
    {
      "params" : [
        {
          "name" : "from",
          "type" : "date",
          "optional" : false,
          "description" : "Inclusive lower bound of the range (`[from, to)`). If `null`, the function returns `null`."
        },
        {
          "name" : "to",
          "type" : "date",
          "optional" : false,
          "description" : "Exclusive upper bound of the range (`[from, to)`). If `null`, the function returns `null`."
        }
      ],
      "variadic" : false,
      "returnType" : "date_range"
    }
  ],
  "examples" : [
    "ROW from_dt = TO_DATETIME(\"2000-01-01T00:00:00.000Z\"), to_dt = TO_DATETIME(\"2010-01-01T00:00:00.000Z\")\n| EVAL r = TO_RANGE(from_dt, to_dt)\n| KEEP r"
  ],
  "preview" : true,
  "snapshot_only" : false
}

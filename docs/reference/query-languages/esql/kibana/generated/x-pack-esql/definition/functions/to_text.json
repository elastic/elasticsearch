{
  "comment" : "This is generated by ESQL's ToTextTests. Do not edit it. See docs/reference/query-languages/esql/README.md for how to regenerate it.",
  "type" : "scalar",
  "name" : "to_text",
  "description" : "Converts a value to an expression of type `TEXT`. This is different from the `TO_STRING` function, which converts to\n`KEYWORD`. `TEXT` and `KEYWORD` data types are treated in ES|QL almost the same, the main difference is that\n`TEXT` is considered to be analyzed, while `KEYWORD` is not.\nThis matters for functions like `MATCH` which will treat these data types\ndifferently.",
  "signatures" : [
    {
      "params" : [
        {
          "name" : "field",
          "type" : "keyword",
          "optional" : false,
          "description" : "Input value. The input can be a single- or multi-valued column or an expression."
        }
      ],
      "variadic" : false,
      "returnType" : "text"
    },
    {
      "params" : [
        {
          "name" : "field",
          "type" : "text",
          "optional" : false,
          "description" : "Input value. The input can be a single- or multi-valued column or an expression."
        }
      ],
      "variadic" : false,
      "returnType" : "text"
    }
  ],
  "examples" : [
    "ROW a = [\"this\", \"is\", \"a\", \"text\"]\n| EVAL text_a = TO_TEXT(a)"
  ],
  "preview" : false,
  "snapshot_only" : true
}

role1:
  cluster: ALL
  indices:
    'idx1,idx2': READ
    idx3: crud

role1.ab:
  cluster: ALL

role2:
  cluster: ALL, MONITOR

role3:
  indices:
    '/.*_.*/': READ, WRITE

#dadfad
role4:
  cluster:
  indices:
    #adfldkkd
    'idx2':
    '': READ
    'idx1': []

# role with run_as permissions only
role_run_as:
  run_as: "user1,user2"

# role with more than run_as
role_run_as1:
  run_as: [user1, user2]

role_fields:
  indices:
    'field_idx':
      privileges: READ
      fields:
        - foo
        - boo

role_query:
  indices:
    'query_idx':
      privileges: READ
      query: '{ "match_all": {} }'

role_query_fields:
  indices:
    'query_fields_idx':
      privileges: READ
      query: '{ "match_all": {} }'
      fields:
        - foo
        - boo
"Get missing role":
  - do:
      catch:  missing
      shield.get_role:
        name: 'foo'

---
"Get missing (multiple) roles":
  - do:
      catch:  missing
      shield.get_role:
        name: [ 'foo', 'bar' ]

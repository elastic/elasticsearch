---
"Test geoip stats":
  - do:
      ingest.geo_ip_stats: {}
  - match: { stats.successful_downloads: 0 }
  - match: { stats.failed_downloads: 0 }
  - match: { stats.skipped_updates: 0 }
  - match: { stats.databases_count: 0 }
  - match: { stats.total_download_time: 0 }
  - match: { nodes: {} }

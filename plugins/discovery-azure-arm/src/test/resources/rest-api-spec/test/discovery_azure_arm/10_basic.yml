# Integration tests for Azure ARM Discovery component
#
"Discovery Azure ARM loaded":
    - do:
        cluster.state: {}

    # Get master node id
    - set: { master_node: master }

    - do:
        nodes.info: {}

    - match:  { nodes.$master.plugins.0.name: discovery-azure-arm  }

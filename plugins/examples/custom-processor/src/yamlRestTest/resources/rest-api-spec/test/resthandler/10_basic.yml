"Custom processor is present":
  - do:
      ingest.put_pipeline:
        id: pipeline1
        body:  >
          {
            "processors": [
              {
                "repeat" : {}
              }
            ]
          }
  - match: { acknowledged: true }

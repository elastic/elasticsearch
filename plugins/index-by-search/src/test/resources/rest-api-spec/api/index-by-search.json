{
  "index_by_search": {
    "documentation": "https://www.elastic.co/guide/en/elasticsearch/plugins/master/plugins-index-by-search.html",
    "methods": ["POST"],
    "url": {
      "path": "/{index}/_index_by_search",
      "paths": ["/{index}/_index_by_search", "/{index}/{type}/_index_by_search"],
      "parts": {
        "index": {
          "type" : "list",
          "required": true,
          "description" : "A comma-separated list of indices to restrict the operation; use `_all` to perform the operation on all indices"
        },
        "type": {
          "type" : "list",
          "description" : "A comma-separated list of types to restrict the operation"
        }
      },
      "params": {
        "q": {
          "type" : "string",
          "description" : "Query in the Lucene query string syntax"
        }
      }
    },
    "body": {
      "description" : "A query to restrict the operation specified with the Query DSL"
    }
  }
}

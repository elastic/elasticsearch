---
"invalid conflicts fails":
  - do:
      index:
        index:   test
        type:    test
        id:      1
        body:    { "text": "test" }
  - do:
      catch: /conflicts may only be .* but was \[cat\]/
      reindex:
        index: test
        conflicts: cat
  - do:
      catch: /conflicts may only be .* but was \[cat\]/
      reindex:
        index: test
        body:
          conflicts: cat


---
"invalid version_type fails":
  - do:
      index:
        index:   test
        type:    test
        id:      1
        body:    { "text": "test" }
  - do:
      catch: /version_type may only be .* but was \[cat\]/
      reindex:
        index: test
        version_type: cat
  - do:
      catch: /version_type may only be .* but was \[cat\]/
      reindex:
        index: test
        body:
          version_type: cat

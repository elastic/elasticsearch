ext.stableApiSince = "8.6.0"

// TODO: got this idea from JDBC gradle config, seems to make us pull from maven
group = 'org.elasticsearch.qa.stable-api.plugin-api'

apply plugin: 'elasticsearch.stable-api'

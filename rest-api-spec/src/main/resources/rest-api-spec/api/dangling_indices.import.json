{
  "dangling_indices.import": {
    "documentation": {
      "description": "Imports the specified dangling index"
    },
    "stability": "stable",
    "url": {
      "paths": [
        {
          "path": "/_dangling/{index_uuid}",
          "methods": [
            "POST"
          ],
          "parts": {
            "index_uuid": {
              "type": "string",
              "description": "The UUID of the dangling index"
            }
          }
        }
      ]
    },
    "params": {
      "accept_data_loss": {
        "type": "boolean",
        "description": "Must be set to true in order to import the dangling index"
      },
      "node_id": {
        "type": "string",
        "description": "Specifies the node ID where the dangling index to import resides, in the case where the index exists on multiple nodes"
      }
    }
  }
}

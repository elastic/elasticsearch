{
  "slm.delete_lifecycle": {
    "documentation": {
      "url": "https://www.elastic.co/guide/en/elasticsearch/reference/8.19/slm-api-delete-policy.html",
      "description": "Delete a policy"
    },
    "stability": "stable",
    "visibility": "public",
    "headers": {
      "accept": [
        "application/json"
      ]
    },
    "url": {
      "paths": [
        {
          "path": "/_slm/policy/{policy_id}",
          "methods": [
            "DELETE"
          ],
          "parts": {
            "policy_id": {
              "type": "string",
              "description": "The id of the snapshot lifecycle policy to remove"
            }
          }
        }
      ]
    },
    "params": {
      "master_timeout": {
        "type": "time",
        "default": "30s",
        "description": "Explicit operation timeout for connection to master node"
      },
      "timeout": {
        "type": "time",
        "default": "30s",
        "description": "Explicit operation timeout"
      }
    }
  }
}

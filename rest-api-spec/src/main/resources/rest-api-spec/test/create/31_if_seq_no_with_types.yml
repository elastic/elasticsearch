---
"Create with if seq no set":

 - do:
      catch: bad_request
      create:
        index:          test
        type:           test
        id:             3
        body:           { foo: bar }
        if_seq_no:      5
        if_primary_term: 1

 - match: { status: 400 }
 - match: { error.type: action_request_validation_exception }
 - match: { error.reason: "Validation Failed: 1: create operations do not support compare and set. use index instead;" }

---
"Basic with types":

  - do:
      index:
        index: test_1
        type:  test
        id:    1
        body:  { "foo": "bar" }

  - do:
      warnings:
        - '[types removal] Specifying types in get_source requests is deprecated.'
      get_source:
        index: test_1
        type:  test
        id:    1

  - match: { '':  { foo: bar } }

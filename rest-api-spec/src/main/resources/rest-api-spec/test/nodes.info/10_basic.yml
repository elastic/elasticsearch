setup:
  - skip:
      features: [unique_key]
---
"node_info test":
  - do:
      nodes.info:
        node_id: _master
        metric: process
  - set: { nodes._unique_key_: master }

  - do:
      nodes.info: {}

  - is_true: nodes
  - is_true: cluster_name
  - is_true: nodes.$master.roles

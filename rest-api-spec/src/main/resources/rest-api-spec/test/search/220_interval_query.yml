setup:
  - skip:
    - version: " - 6.99.99"
    - reason:  "Implemented in 7.0"

  - do:
        indices.create:
          index:  test
          body:
            mappings:
              test:
                properties:
                  text:
                    type: text
  - do:
        index:
            index:  test
            type:   test
            id:     1
            body:   { text: some short words and a stupendously long one }

  - do:
        indices.refresh:
          index: [test]

---
"cluster health basic test":
  - skip:
      version: "- 8.1.99"
      reason: "health was only added in 8.2.0"

  - do:
      health: {}

  - is_true: cluster_name
  - match:   { status: "GREEN" }
  - match:   { impacts: [] }
  - match:   { components: {} }

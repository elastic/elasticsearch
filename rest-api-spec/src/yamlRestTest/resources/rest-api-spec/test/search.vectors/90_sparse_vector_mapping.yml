setup:
  - skip:
      version: " - 8.10.99"
      reason: "sparse_vector field type reintroduced in 8.11"

---
"Create a mapping with default values":
  - do:
      indices.create:
          index: test
          body:
            settings:
              number_of_replicas: 0
            mappings:
              properties:
                text:
                   type: text
                ml.tokens:
                   type: sparse_vector

  - match: { acknowledged: true }

---
"Test benchmark abort":

  - skip:
        version:     "0 - 9999"
        reason:      Disabled until nodes run with node.bench set to true

  - do:
        abort_benchmark:
            name: my_benchmark
        catch: request


---
"Test get watch api with missing watch":
  - do:
      cluster.health:
          wait_for_status: green


  - do:
      catch: missing
      watcher.get_watch:
        id: "missing_watch"
  - match: { found : false}
  - match: { _id: "missing_watch" }

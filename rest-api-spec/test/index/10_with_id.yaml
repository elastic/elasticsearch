---
"Index with ID":

 - do:
      index:
          index:  weird-index-中文
          type:   weird.type
          id:     1
          body:   { foo: bar }

 - is_true:   ok
 - match:   { _index:   weird-index-中文 }
 - match:   { _type:    weird.type }
 - match:   { _id:      "1"}
 - match:   { _version: 1}

 - do:
      get:
          index:  weird-index-中文
          type:   weird.type
          id:     1

 - match:   { _index:   weird-index-中文 }
 - match:   { _type:    weird.type }
 - match:   { _id:      "1"}
 - match:   { _version: 1}
 - match:   { _source: { foo: bar }}


---
"Regression tests for #3363 and #3364":
  - skip:
      version:  "0 - 0.90.2"
      reason:   "Issues only fixed after 0.90.2"

  - do:
      catch: request
      indices.delete_alias: {}

  - do:
      catch: request
      indices.delete_alias:
        index: test

  - do:
      catch: request
      indices.delete_alias:
        name: test

  

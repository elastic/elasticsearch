version: '3'
services:
  minio-fixture:
    build:
      context: .
      args:
        bucket: "bucket"
        accessKey: "s3_minio_access_key"
        secretKey: "s3_minio_secret_key"
      dockerfile: Dockerfile
    ports:
      - "9000"
    command: ["server", "/minio/data"]
  minio-fixture-other:
    build:
      context: .
      args:
        bucket: "bucket"
        accessKey: "s3_minio_access_key"
        secretKey: "s3_minio_secret_key"
      dockerfile: Dockerfile
    ports:
      - "9000"
    command: ["server", "/minio/data"]

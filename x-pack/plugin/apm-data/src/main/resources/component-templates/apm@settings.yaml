version: ${xpack.apmdata.template.version}
_meta:
  description: Default settings for all APM data streams
  managed: true
template:
  settings:
    index:
      sort:
        field: "@timestamp"
        order: desc

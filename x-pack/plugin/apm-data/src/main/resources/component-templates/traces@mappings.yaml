---
version: ${xpack.apmdata.template.version}
_meta:
  description: Default mappings for traces data streams
  managed: true
template:
  mappings:
    properties:
      data_stream.type:
        type: constant_keyword
        value: traces

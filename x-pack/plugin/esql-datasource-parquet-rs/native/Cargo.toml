[package]
name = "esql-parquet-rs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
arrow = { version = "58", features = ["ffi"] }
parquet = { version = "58", features = ["arrow"] }
jni = "0.22"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

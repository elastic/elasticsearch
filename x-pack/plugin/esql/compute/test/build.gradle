apply plugin: 'elasticsearch.build'

base {
  archivesName = 'x-pack-esql-compute-test'
}

dependencies {
  compileOnly project(':test:framework')
  compileOnly project(xpackModule('esql:compute'))
}


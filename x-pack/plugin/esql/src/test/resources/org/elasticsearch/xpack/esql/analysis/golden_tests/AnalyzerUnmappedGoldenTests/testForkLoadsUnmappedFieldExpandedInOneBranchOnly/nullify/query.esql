SET unmapped_fields="nullify"; FROM partial_mapping_sample_data
| FORK (MV_EXPAND unmapped_message)
       (WHERE message == "42")
| KEEP _fork, message, unmapped_message
| SORT _fork, message, unmapped_message

FROM employees,
     (FROM languages),
     (FROM languages),
     (FROM languages),
     (FROM languages),
     (FROM languages),
     (FROM languages),
     (FROM languages),
     (FROM languages),
     (FROM languages)

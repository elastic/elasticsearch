setup:
  - do:
      fleet.post_secret:
        body: '"value": "test secret"'
  # return id?

---
teardown:
  - do:
      fleet.delete_secret:
        id: "blah" # actual id

---
"Get Fleet Secret":
  - do:
      fleet.get_secret:
        id: "blah" # actual id
  - match: { value: "test secret" }

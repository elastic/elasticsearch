apply plugin: 'elasticsearch.internal-java-rest-test'

dependencies {
  javaRestTestImplementation project(xpackModule('core'))
  javaRestTestImplementation testArtifact(project(xpackModule('core')))
  javaRestTestImplementation project(xpackModule('inference'))
  javaRestTestImplementation testArtifact(project(xpackModule('inference')))
  javaRestTestImplementation project(':modules:reindex')
  javaRestTestImplementation project(':x-pack:plugin:inference:qa:test-service-plugin')
}

tasks.named('javaRestTest') {
  usesDefaultDistribution("to be triaged")
}

ALL-UNNAMED:
  - set_https_connection_properties # potentially required by apache.httpcomponents
# Commented out for testing
#  - write_all_properties # unclear if still needed because of problems in unbound LDAP library?

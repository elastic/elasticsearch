# OTel Data Plugin

## Template Version

The `version` field in `src/main/resources/resources.yaml` controls whether component templates
and index templates are updated on Elasticsearch upgrade. You **must** increment this version
if you change any existing template listed in that file.

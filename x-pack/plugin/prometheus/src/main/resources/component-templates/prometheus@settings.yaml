version: ${xpack.prometheus.template.version}
_meta:
  description: Default settings for Prometheus data streams
  managed: true
template:
  data_stream_options:
    failure_store:
      enabled: true

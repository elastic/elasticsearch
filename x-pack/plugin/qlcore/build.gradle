apply plugin: 'elasticsearch.internal-es-plugin'
apply plugin: 'elasticsearch.internal-test-artifact'

esplugin {
  name 'x-pack-qlcore'
  description 'Elasticsearch infrastructure plugin for ESQL for Elasticsearch'
  classname 'org.elasticsearch.xpack.qlcore.plugin.QlCorePlugin'
  extendedPlugins = ['x-pack-core']
}

base {
  archivesName = 'x-pack-qlcore'
}

dependencies {
  api "org.antlr:antlr4-runtime:${versions.antlr4}"
  api project(path: xpackModule('mapper-version'))
  compileOnly project(path: xpackModule('core'))
  testApi(project(xpackModule('qlcore:test-fixtures'))) {
    exclude group: 'org.elasticsearch.plugin', module: 'qlcore'
  }
  testImplementation project(':test:framework')
  testImplementation(testArtifact(project(xpackModule('core'))))
}

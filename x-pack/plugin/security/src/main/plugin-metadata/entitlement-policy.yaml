org.elasticsearch.security:
  - set_https_connection_properties # for CommandLineHttpClient
io.netty.transport:
  - network:
      actions:
        - listen
io.netty.common:
  - network:
      actions:
        - accept
        - connect

{
  "ml.delete_filter": {
    "methods": [ "DELETE" ],
    "stability": "stable",
    "url": {
      "paths": [ "/_ml/filters/{filter_id}" ],
      "parts": {
        "filter_id": {
          "type" : "string",
          "required" : true,
          "description" : "The ID of the filter to delete"
        }
      }
    },
    "body": null
  }
}

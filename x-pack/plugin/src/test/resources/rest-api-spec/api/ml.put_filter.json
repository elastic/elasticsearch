{
  "ml.put_filter": {
    "methods": [ "PUT" ],
    "url": {
      "paths": [ "/_ml/filters/{filter_id}" ],
      "parts": {
        "filter_id": {
          "type": "string",
          "required": true,
          "description": "The ID of the filter to create"
        }
      }
    },
    "body": {
      "description" : "The filter details",
      "required" : true
    }
  }
}

{
  "xpack.rollup.stop_job": {
    "documentation": "",
    "methods": [ "POST" ],
    "url": {
      "path": "/_xpack/rollup/job/{id}/_stop",
      "paths": [ "/_xpack/rollup/job/{id}/_stop" ],
      "parts": {
        "id": {
          "type": "string",
          "required": true,
          "description": "The ID of the job to stop"
        }
      },
      "params": {
        "wait_for_stopped": {
          "type": "time",
          "required": false,
          "description": "If set, the API will block for (at maximum) the specified duration while waiting for the job to stop. If unset, returns immediately."
        }
      }
    }
  }
}

# Required for CatIndicesWithSecurityIT
cat_test_role:
  cluster:
    - monitor
    - cluster:admin/settings/update
  indices:
    - names: [ "index_allowed" ]
      privileges: [ "read", "write", "monitor" ]

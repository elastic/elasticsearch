setup:
  - requires:
      test_runner_features: [ allowed_warnings, capabilities ]
  - do:
      allowed_warnings:
        - "Parameter [default_metric] is deprecated and will be removed in a future version"
      indices.create:
        index:  aggregate_metric_test
        body:
          mappings:
            properties:
              metric:
                type: aggregate_metric_double
                metrics: [sum, value_count]
                default_metric: value_count

  - do:
      bulk:
        index: aggregate_metric_test
        refresh: true
        body:
          - '{"index": {}}'
          - '{"metric": {"sum": 10000.45, "value_count": 100} }'
          - '{"index": {}}'
          - '{"metric": {"sum": 60, "value_count": 20} }'
          - '{"index": {}}'
          - '{"metric": {"sum": -400.50, "value_count": 1000} }'
          - '{"index": {}}'
          - '{"metric": {"sum": 1, "value_count": 20} }'
          - '{"index": {}}'
          - '{"metric": {"sum": -100, "value_count": 40} }'
---
"Test sum agg":
  - do:
      search:
        index: aggregate_metric_test
        size: 0
        body:
          aggs:
            sum_agg:
              sum:
                field: metric

  - match: { hits.total.value: 5 }
  - match: { aggregations.sum_agg.value: 9560.95}

---
"Test sum agg with query":
  - requires:
      capabilities:
        - method: POST
          path: /_search
          capabilities: [ aggregate_metric_double_defaults_to_average ]
      reason: "Using average as default for aggregate metric double was added in 9.14"
  - do:
      search:
        index: aggregate_metric_test
        size: 0
        body:
          query:
            range:
              metric:
                gte: 2
                lte: 200
          aggs:
            sum_agg:
              sum:
                field: metric

  - match: { hits.total.value: 2 }
  - match: { aggregations.sum_agg.value: 10060.45}

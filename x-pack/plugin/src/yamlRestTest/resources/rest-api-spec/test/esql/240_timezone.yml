---
setup:
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ global_timezone_parameter_with_output ]
      test_runner_features: [ capabilities ]
      reason: "Check timezone capability"
  - do:
      bulk:
        index: tzs
        refresh: true
        body:
          - { "index": { } }
          - { "@timestamp": "2025-05-31T00:00:00Z" }
          - { "index": { } }
          - { "@timestamp": "2025-05-31T01:00:00Z" }
          - { "index": { } }
          - { "@timestamp": "2025-05-31T22:00:00Z" }
          - { "index": { } }
          - { "@timestamp": "2025-05-31T23:00:00Z" }

---
Default UTC:
  - do:
      esql.query:
        body:
          query: |
            FROM tzs
            | EVAL
                hour=DATE_EXTRACT("hour_of_day", @timestamp),
                day_name=DAY_NAME(@timestamp),
                month_name=MONTH_NAME(@timestamp)
            | SORT @timestamp ASC
            | LIMIT 5

  - length: { columns: 4 }
  - match: { columns.0.name: "@timestamp" }
  - match: { columns.1.name: "hour" }
  - match: { columns.2.name: "day_name" }
  - match: { columns.3.name: "month_name" }

  - length: { values: 4 }
  - match: { values.0.0: "2025-05-31T00:00:00.000Z" }
  - match: { values.0.1: 0 }
  - match: { values.0.2: "Saturday" }
  - match: { values.0.3: "May" }
  - match: { values.1.0: "2025-05-31T01:00:00.000Z" }
  - match: { values.1.1: 1 }
  - match: { values.1.2: "Saturday" }
  - match: { values.1.3: "May" }
  - match: { values.2.0: "2025-05-31T22:00:00.000Z" }
  - match: { values.2.1: 22 }
  - match: { values.2.2: "Saturday" }
  - match: { values.2.3: "May" }
  - match: { values.3.0: "2025-05-31T23:00:00.000Z" }
  - match: { values.3.1: 23 }
  - match: { values.3.2: "Saturday" }
  - match: { values.3.3: "May" }

---
Paris tz with SET:
  - do:
      esql.query:
        body:
          query: |
            SET time_zone="Europe/Paris";
            FROM tzs
            | EVAL
                hour=DATE_EXTRACT("hour_of_day", @timestamp),
                day_name=DAY_NAME(@timestamp),
                month_name=MONTH_NAME(@timestamp)
            | SORT @timestamp ASC
            | LIMIT 5

  - length: { columns: 4 }
  - match: { columns.0.name: "@timestamp" }
  - match: { columns.1.name: "hour" }
  - match: { columns.2.name: "day_name" }
  - match: { columns.3.name: "month_name" }

  - length: { values: 4 }
  - match: { values.0.0: "2025-05-31T02:00:00.000+02:00" }
  - match: { values.0.1: 2 }
  - match: { values.0.2: "Saturday" }
  - match: { values.0.3: "May" }
  - match: { values.1.0: "2025-05-31T03:00:00.000+02:00" }
  - match: { values.1.1: 3 }
  - match: { values.1.2: "Saturday" }
  - match: { values.1.3: "May" }
  - match: { values.2.0: "2025-06-01T00:00:00.000+02:00" }
  - match: { values.2.1: 0 }
  - match: { values.2.2: "Sunday" }
  - match: { values.2.3: "June" }
  - match: { values.3.0: "2025-06-01T01:00:00.000+02:00" }
  - match: { values.3.1: 1 }
  - match: { values.3.2: "Sunday" }
  - match: { values.3.3: "June" }

---
Paris tz with request parameter:
  - do:
      esql.query:
        body:
          time_zone: "Europe/Paris"
          query: |
            FROM tzs
            | EVAL
                hour=DATE_EXTRACT("hour_of_day", @timestamp),
                day_name=DAY_NAME(@timestamp),
                month_name=MONTH_NAME(@timestamp)
            | SORT @timestamp ASC
            | LIMIT 5

  - length: { columns: 4 }
  - match: { columns.0.name: "@timestamp" }
  - match: { columns.1.name: "hour" }
  - match: { columns.2.name: "day_name" }
  - match: { columns.3.name: "month_name" }

  - length: { values: 4 }
  - match: { values.0.0: "2025-05-31T02:00:00.000+02:00" }
  - match: { values.0.1: 2 }
  - match: { values.0.2: "Saturday" }
  - match: { values.0.3: "May" }
  - match: { values.1.0: "2025-05-31T03:00:00.000+02:00" }
  - match: { values.1.1: 3 }
  - match: { values.1.2: "Saturday" }
  - match: { values.1.3: "May" }
  - match: { values.2.0: "2025-06-01T00:00:00.000+02:00" }
  - match: { values.2.1: 0 }
  - match: { values.2.2: "Sunday" }
  - match: { values.2.3: "June" }
  - match: { values.3.0: "2025-06-01T01:00:00.000+02:00" }
  - match: { values.3.1: 1 }
  - match: { values.3.2: "Sunday" }
  - match: { values.3.3: "June" }

---
Set overrides request parameter:
  - do:
      esql.query:
        body:
          time_zone: "Europe/Paris"
          query: |
            SET time_zone="+04:00";
            FROM tzs
            | EVAL
                hour=DATE_EXTRACT("hour_of_day", @timestamp),
                day_name=DAY_NAME(@timestamp),
                month_name=MONTH_NAME(@timestamp)
            | SORT @timestamp ASC
            | LIMIT 5

  - length: { columns: 4 }
  - match: { columns.0.name: "@timestamp" }
  - match: { columns.1.name: "hour" }
  - match: { columns.2.name: "day_name" }
  - match: { columns.3.name: "month_name" }

  - length: { values: 4 }
  - match: { values.0.0: "2025-05-31T04:00:00.000+04:00" }
  - match: { values.0.1: 4 }
  - match: { values.0.2: "Saturday" }
  - match: { values.0.3: "May" }
  - match: { values.1.0: "2025-05-31T05:00:00.000+04:00" }
  - match: { values.1.1: 5 }
  - match: { values.1.2: "Saturday" }
  - match: { values.1.3: "May" }
  - match: { values.2.0: "2025-06-01T02:00:00.000+04:00" }
  - match: { values.2.1: 2 }
  - match: { values.2.2: "Sunday" }
  - match: { values.2.3: "June" }
  - match: { values.3.0: "2025-06-01T03:00:00.000+04:00" }
  - match: { values.3.1: 3 }
  - match: { values.3.2: "Sunday" }
  - match: { values.3.3: "June" }

---
Paris tz under settings.time_zone:
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "Requires the settings request body block"
  - do:
      esql.query:
        body:
          settings:
            time_zone: "Europe/Paris"
          query: |
            FROM tzs
            | EVAL
                hour=DATE_EXTRACT("hour_of_day", @timestamp),
                day_name=DAY_NAME(@timestamp),
                month_name=MONTH_NAME(@timestamp)
            | SORT @timestamp ASC
            | LIMIT 5

  - length: { columns: 4 }
  - match: { columns.0.name: "@timestamp" }
  - match: { columns.1.name: "hour" }
  - match: { columns.2.name: "day_name" }
  - match: { columns.3.name: "month_name" }

  - length: { values: 4 }
  - match: { values.0.0: "2025-05-31T02:00:00.000+02:00" }
  - match: { values.0.1: 2 }
  - match: { values.0.2: "Saturday" }
  - match: { values.0.3: "May" }
  - match: { values.1.0: "2025-05-31T03:00:00.000+02:00" }
  - match: { values.1.1: 3 }
  - match: { values.1.2: "Saturday" }
  - match: { values.1.3: "May" }
  - match: { values.2.0: "2025-06-01T00:00:00.000+02:00" }
  - match: { values.2.1: 0 }
  - match: { values.2.2: "Sunday" }
  - match: { values.2.3: "June" }
  - match: { values.3.0: "2025-06-01T01:00:00.000+02:00" }
  - match: { values.3.1: 1 }
  - match: { values.3.2: "Sunday" }
  - match: { values.3.3: "June" }

---
SET overrides settings.time_zone:
  # In-query SET wins over the nested settings.time_zone (in-query SET is the most-specific scope).
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "Requires the settings request body block"
  - do:
      esql.query:
        body:
          settings:
            time_zone: "Europe/Paris"
          query: |
            SET time_zone="+04:00";
            FROM tzs
            | EVAL
                hour=DATE_EXTRACT("hour_of_day", @timestamp),
                day_name=DAY_NAME(@timestamp),
                month_name=MONTH_NAME(@timestamp)
            | SORT @timestamp ASC
            | LIMIT 5

  - length: { values: 4 }
  - match: { values.0.0: "2025-05-31T04:00:00.000+04:00" }
  - match: { values.0.1: 4 }
  - match: { values.1.0: "2025-05-31T05:00:00.000+04:00" }
  - match: { values.1.1: 5 }
  - match: { values.2.0: "2025-06-01T02:00:00.000+04:00" }
  - match: { values.2.1: 2 }
  - match: { values.3.0: "2025-06-01T03:00:00.000+04:00" }
  - match: { values.3.1: 3 }

---
conflicting time_zone at top level and under settings is rejected:
  # The same setting supplied via both the canonical settings.{} block and the legacy top-level field with
  # DIFFERENT values is a client bug, not an override — reject with a 400 rather than silently picking a winner.
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "Requires the settings request body block"
  - do:
      catch: bad_request
      esql.query:
        body:
          time_zone: "+04:00"
          settings:
            time_zone: "Europe/Paris"
          query: "FROM tzs | LIMIT 1"
  - match: { error.root_cause.0.reason: "/.*Setting..time_zone..has.conflicting.values.*under..settings./" }

---
conflicting time_zone is rejected regardless of JSON field order:
  # Same conflict but with the legacy field appearing AFTER settings in the JSON. The check runs after the
  # whole body is parsed, so it does not depend on parse order.
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "Requires the settings request body block"
  - do:
      catch: bad_request
      esql.query:
        body:
          settings:
            time_zone: "Europe/Paris"
          time_zone: "+04:00"
          query: "FROM tzs | LIMIT 1"
  - match: { error.root_cause.0.reason: "/.*Setting..time_zone..has.conflicting.values.*under..settings./" }

---
identical time_zone at top level and under settings is accepted:
  # The same value in both places is a fair migration path — a client moving a field from the top level into
  # settings.{} may carry it at both surfaces during the transition. Accept it, no error.
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "Requires the settings request body block"
  - do:
      esql.query:
        body:
          time_zone: "Europe/Paris"
          settings:
            time_zone: "Europe/Paris"
          query: |
            FROM tzs
            | EVAL hour=DATE_EXTRACT("hour_of_day", @timestamp)
            | SORT @timestamp ASC
            | LIMIT 1

  - length: { values: 1 }
  - match: { values.0.0: "2025-05-31T02:00:00.000+02:00" }
  - match: { values.0.1: 2 }

---
Unknown key under settings is rejected:
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "Requires the settings request body block"
  - do:
      catch: bad_request
      esql.query:
        body:
          settings:
            no_such_setting: "x"
          query: "FROM tzs | LIMIT 1"
  - match: { error.root_cause.0.reason: "/.*Unknown.setting..no_such_setting..under..settings./" }

---
SET-only setting under settings is rejected:
  # unmapped_fields exists in the registry but opted out of body exposure. It must be set via in-query SET,
  # not via the request body.
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "Requires the settings request body block"
  - do:
      catch: bad_request
      esql.query:
        body:
          settings:
            unmapped_fields: "NULLIFY"
          query: "FROM tzs | LIMIT 1"
  - match: { error.root_cause.0.reason: "/.*Setting..unmapped_fields..is.not.exposed.as.a.request.body.parameter.*/" }

---
Unknown in-query SET key is rejected:
  - requires:
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ query_settings_request_body ]
      test_runner_features: [ capabilities ]
      reason: "gated on the newest settings-body behavior; unknown SET keys are rejected (as they always were)"
  - do:
      catch: bad_request
      esql.query:
        body:
          query: |
            SET no_such_setting="x";
            FROM tzs | LIMIT 1
  - match: { error.root_cause.0.reason: "/.*Unknown.setting..no_such_setting..*/" }

---
Function parameter with SET:
  - do:
      esql.query:
        body:
          time_zone: "Europe/Paris"
          query: |
            SET time_zone="+04:00";
            ROW @timestamp = DATE_PARSE("2025-05-31T04:00:00.000", { "time_zone": "+08:00" })
            | EVAL
                hour=DATE_EXTRACT("hour_of_day", @timestamp),
                day_name=DAY_NAME(@timestamp),
                month_name=MONTH_NAME(@timestamp)
            | SORT @timestamp ASC
            | LIMIT 5

  - length: { columns: 4 }
  - match: { columns.0.name: "@timestamp" }
  - match: { columns.1.name: "hour" }
  - match: { columns.2.name: "day_name" }
  - match: { columns.3.name: "month_name" }

  - length: { values: 1 }
  - match: { values.0.0: "2025-05-31T00:00:00.000+04:00" }
  - match: { values.0.1: 0 }
  - match: { values.0.2: "Saturday" }
  - match: { values.0.3: "May" }

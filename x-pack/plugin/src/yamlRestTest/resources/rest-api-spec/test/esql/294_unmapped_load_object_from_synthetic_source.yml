---
setup:
  - requires:
      test_runner_features: [ capabilities ]
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ optional_fields_v5, optional_fields_fix_load_object_from_synthetic_source ]
      reason: "Loading an unmapped object field as a keyword from synthetic source must return null, not trip an assertion (issues #156381, #156433)"
  #
  # Synthetic source with dynamic:false stores unmapped fields in _ignored_source. "unmapped" is entirely unmapped, so its whole
  # object is stored under one parent (the #156433 START_OBJECT case). "network" and "network.eth0" are mapped objects but their
  # bytes_in / eth0.tx leaves are not, so those land in _ignored_source under different parents - "network" and "network.eth0" -
  # the multi-parent grouping of #156381. Referencing either object as a keyword must yield null; the scalar leaves still load.
  #
  - do:
      indices.create:
        index: synthetic_objects
        body:
          settings:
            index:
              mapping:
                source:
                  mode: synthetic
          mappings:
            dynamic: false
            properties:
              id:
                type: keyword
              network:
                properties:
                  eth0:
                    properties:
                      rx:
                        type: integer
  - do:
      index:
        index: synthetic_objects
        id: "1"
        refresh: true
        body:
          id: "1"
          unmapped:
            nested: "a"
          network:
            bytes_in: 10
            eth0:
              tx: 5
              rx: 7

---
teardown:
  - do:
      indices.delete:
        index: synthetic_objects

---
'unmapped object field from synthetic source loads as null':
  - do:
      esql.query:
        body:
          query: 'SET unmapped_fields="load"; FROM synthetic_objects | KEEP id, unmapped, unmapped.nested | LIMIT 5'
  - match: { columns.0.name: id }
  - match: { columns.1.name: unmapped }
  - match: { columns.1.type: keyword }
  - match: { columns.2.name: unmapped.nested }
  - match: { columns.2.type: keyword }
  - match: { values.0: [ "1", null, "a" ] }

---
'unmapped multi-parent object field from synthetic source loads as null under load_all':
  - requires:
      test_runner_features: [ capabilities ]
      capabilities:
        - method: POST
          path: /_query
          parameters: [ ]
          capabilities: [ optional_fields_load_all ]
      reason: "SET unmapped_fields=\"LOAD_ALL\" resolves the mapped object 'network' to an unmapped keyword"
  - do:
      esql.query:
        body:
          query: 'SET unmapped_fields="LOAD_ALL"; FROM synthetic_objects | KEEP id, network, network.eth0.rx | LIMIT 5'
  - match: { columns.0.name: id }
  - match: { columns.1.name: network }
  - match: { columns.1.type: keyword }
  - match: { columns.2.name: network.eth0.rx }
  - match: { columns.2.type: integer }
  - match: { values.0: [ "1", null, 7 ] }

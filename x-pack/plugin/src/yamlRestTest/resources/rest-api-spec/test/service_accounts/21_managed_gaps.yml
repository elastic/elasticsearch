---
setup:
  - skip:
      features: headers

  - do:
      cluster.health:
        wait_for_status: yellow

  - do:
      security.put_role:
        name: "managed_yaml_monitor"
        body: >
          {
            "cluster": ["monitor"]
          }

  - do:
      security.put_role:
        name: "service_account_admin"
        body: >
          {
            "cluster": ["manage_service_account"]
          }

  - do:
      security.put_user:
        username: "service_account_admin_user"
        body: >
          {
            "password": "x-pack-test-password",
            "roles": ["service_account_admin"]
          }

---
teardown:
  - do:
      security.delete_service_token:
        namespace: elastic
        service: fleet-server
        name: gap-elastic-token
        ignore: 404

  - do:
      security.delete_service_token:
        namespace: yaml_poc
        service: gap_worker
        name: gap-managed-token
        ignore: 404

  - do:
      security.delete_service_token:
        namespace: yaml_poc
        service: disabled_worker
        name: disabled-token
        ignore: 404

  - do:
      security.delete_service_token:
        namespace: yaml_poc
        service: role_def_worker
        name: role-def-token
        ignore: 404

  - do:
      security.delete_managed_service_account:
        namespace: yaml_poc
        service: gap_worker
        ignore: 404

  - do:
      security.delete_managed_service_account:
        namespace: yaml_poc
        service: disabled_worker
        ignore: 404

  - do:
      security.delete_managed_service_account:
        namespace: yaml_poc
        service: role_def_worker
        ignore: 404

  - do:
      security.delete_user:
        username: "service_account_admin_user"
        ignore: 404

  - do:
      security.delete_role:
        name: "service_account_admin"
        ignore: 404

  - do:
      security.delete_role:
        name: "managed_yaml_monitor"
        ignore: 404

---
"Test manage_service_account privilege boundary":
  - do:
      catch: forbidden
      headers:
        Authorization: "Basic c2VydmljZV9hY2NvdW50X2FkbWluX3VzZXI6eC1wYWNrLXRlc3QtcGFzc3dvcmQ="
      security.put_managed_service_account:
        namespace: yaml_poc
        service: gap_worker
        body: >
          {
            "roles": ["managed_yaml_monitor"],
            "enabled": true
          }

  - do:
      security.put_managed_service_account:
        namespace: yaml_poc
        service: gap_worker
        body: >
          {
            "roles": ["managed_yaml_monitor"],
            "enabled": true
          }

  - do:
      catch: forbidden
      headers:
        Authorization: "Basic c2VydmljZV9hY2NvdW50X2FkbWluX3VzZXI6eC1wYWNrLXRlc3QtcGFzc3dvcmQ="
      security.create_service_token:
        namespace: yaml_poc
        service: gap_worker
        name: gap-managed-token

  - do:
      catch: forbidden
      headers:
        Authorization: "Basic c2VydmljZV9hY2NvdW50X2FkbWluX3VzZXI6eC1wYWNrLXRlc3QtcGFzc3dvcmQ="
      security.delete_managed_service_account:
        namespace: yaml_poc
        service: gap_worker

  - do:
      headers:
        Authorization: "Basic c2VydmljZV9hY2NvdW50X2FkbWluX3VzZXI6eC1wYWNrLXRlc3QtcGFzc3dvcmQ="
      security.create_service_token:
        namespace: elastic
        service: fleet-server
        name: gap-elastic-token
  - is_true: created
  - match: { "token.name": "gap-elastic-token" }

---
"Test reserved elastic namespace for managed account PUT":
  - do:
      catch: bad_request
      security.put_managed_service_account:
        namespace: elastic
        service: custom
        body: >
          {
            "roles": ["managed_yaml_monitor"],
            "enabled": true
          }
  - match: { error.type: action_request_validation_exception }

---
"Test disabled managed account":
  - do:
      security.put_managed_service_account:
        namespace: yaml_poc
        service: disabled_worker
        body: >
          {
            "roles": ["managed_yaml_monitor"],
            "enabled": true
          }

  - do:
      security.create_service_token:
        namespace: yaml_poc
        service: disabled_worker
        name: disabled-token
  - set: { "token.value": disabled_service_token }

  - do:
      security.put_managed_service_account:
        namespace: yaml_poc
        service: disabled_worker
        body: >
          {
            "roles": ["managed_yaml_monitor"],
            "enabled": false
          }
  - match: { created: false }

  - do:
      catch: unauthorized
      headers:
        Authorization: Bearer ${disabled_service_token}
      security.authenticate: {}

  - do:
      catch: bad_request
      security.create_service_token:
        namespace: yaml_poc
        service: disabled_worker
        name: disabled-token-2

---
"Test native role definition change affects managed authentication":
  - do:
      security.put_role:
        name: "managed_yaml_monitor"
        body: >
          {
            "cluster": ["monitor"]
          }

  - do:
      security.put_managed_service_account:
        namespace: yaml_poc
        service: role_def_worker
        body: >
          {
            "roles": ["managed_yaml_monitor"],
            "enabled": true
          }

  - do:
      security.create_service_token:
        namespace: yaml_poc
        service: role_def_worker
        name: role-def-token
  - set: { "token.value": role_def_service_token }

  - do:
      headers:
        Authorization: Bearer ${role_def_service_token}
      security.has_privileges:
        user: null
        body: >
          {
            "cluster": ["monitor", "manage_own_api_key"]
          }
  - match: { "cluster.monitor": true }
  - match: { "cluster.manage_own_api_key": false }

  - do:
      security.put_role:
        name: "managed_yaml_monitor"
        body: >
          {
            "cluster": ["manage_own_api_key"]
          }

  - do:
      headers:
        Authorization: Bearer ${role_def_service_token}
      security.authenticate: {}
  - match: { roles: ["managed_yaml_monitor"] }

  - do:
      headers:
        Authorization: Bearer ${role_def_service_token}
      security.has_privileges:
        user: null
        body: >
          {
            "cluster": ["monitor", "manage_own_api_key"]
          }
  - match: { "cluster.monitor": false }
  - match: { "cluster.manage_own_api_key": true }

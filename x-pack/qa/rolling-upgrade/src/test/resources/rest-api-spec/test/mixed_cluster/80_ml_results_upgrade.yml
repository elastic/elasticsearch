---
"Verify jobs exist":
  - do:
      xpack.ml.get_jobs:
        job_id: old-cluster-job-to-upgrade
  - match: { count: 1 }

  - do:
      xpack.ml.get_jobs:
        job_id: old-cluster-job-to-upgrade-custom
  - match: { count: 1 }
